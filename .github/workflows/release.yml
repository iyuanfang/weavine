name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Cache Electron binary
        uses: actions/cache@v4
        with:
          path: ~/.electron
          key: ${{ runner.os }}-electron-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libfuse2 fakeroot dpkg-dev

      - name: Cache electron-builder
        uses: actions/cache@v4
        with:
          path: ~/.cache/electron-builder
          key: ${{ runner.os }}-builder-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create SQLite database
        run: |
          pnpm exec prisma db push --accept-data-loss
          pnpm exec prisma generate
        env:
          DATABASE_URL: 'file:./dev.db'

      - name: Cache Next.js build
        uses: actions/cache@v4
        with:
          path: .next/cache
          key: ${{ runner.os }}-next-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-next-

      - name: Build Next.js standalone
        run: pnpm exec next build
        env:
          IS_DESKTOP: 'true'
          DATABASE_URL: 'file:./dev.db'
          NODE_OPTIONS: '--max-old-space-size=4096'

      - name: Copy Prisma engines to standalone
        run: node scripts/postbuild-desktop.mjs

      - name: Build Electron package
        id: build_electron
        shell: bash
        run: |
          if [[ '${{ runner.os }}' == 'Linux' ]]; then
            # Step 1: build unpacked dir (reliable)
            ./node_modules/.bin/electron-builder --linux dir --publish never > ebuilder.log 2>&1
            EB_EXIT=$?
            cat ebuilder.log
            if [[ $EB_EXIT -ne 0 ]]; then exit $EB_EXIT; fi
            # Step 2: package into tar.gz from the prepared dir
            echo '--- Packaging into tar.gz ---'
            ./node_modules/.bin/electron-builder --linux tar.gz --prepackaged release/linux-unpacked --publish never 2>&1 | tee -a ebuilder.log
            EB_EXIT=$?
          else
            ./node_modules/.bin/electron-builder --publish never > ebuilder.log 2>&1
            EB_EXIT=$?
          fi
          echo "exit_code=$EB_EXIT" >> "$GITHUB_OUTPUT"
          exit $EB_EXIT
        env:
          CSC_LINK: ''
          CSC_KEY_PASSWORD: ''

      - name: Upload ebuilder log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ebuilder-log-${{ runner.os }}
          path: ebuilder.log

      - uses: actions/upload-artifact@v4
        with:
          name: PRM-${{ runner.os }}
          path: release/*

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release with gh CLI
        run: |
          gh release create "${{ github.ref_name }}" \
            --repo "${{ github.repository }}" \
            --generate-notes \
            --verify-tag || true
          # Upload each artifact file one by one with small delay
          find artifacts -type f | while read f; do
            echo "Uploading: $f"
            gh release upload "${{ github.ref_name }}" "$f" \
              --repo "${{ github.repository }}" --clobber
            sleep 1
          done
        env:
          GH_TOKEN: ${{ github.token }}
